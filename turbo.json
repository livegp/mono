{
  "$schema": "./node_modules/turbo/schema.json",
  "ui": "tui",
  "globalDependencies": ["**/.env", "**/.env.*"],
  "globalEnv": ["NODE_ENV", "VITE_*"],
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false
    },
    "build": {
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", "!**/*.test.*", "!**/*.spec.*"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "check-types": {
      "cache": true,
      "dependsOn": ["^check-types"]
    },
    "test": {
      "cache": true,
      "dependsOn": ["^build"]
    },
    "clean": {
      "cache": false,
      "dependsOn": ["^clean"]
    }
  }
}
